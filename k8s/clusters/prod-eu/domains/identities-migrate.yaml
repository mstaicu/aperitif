apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: identities-migrate
spec:
  interval: 10m
  path: ./k8s/domains/identities/infra/migrate/overlays/live
  force: true
  prune: true
  sourceRef:
    kind: GitRepository
    name: aperitif-repo
  dependsOn:
    - name: identities-db
  wait: true
  timeout: 5m # wait ( wait=true ) 5m for resources to become ready
