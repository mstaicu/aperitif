apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: auth-migrate
spec:
  interval: 10m
  path: ./k8s/domains/auth/infra/migrate/cluster
  prune: true
  wait: true
  timeout: 10m
  sourceRef:
    kind: GitRepository
    name: aperitif-repo
