apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: capabilities-worker
spec:
  interval: 10m
  path: ./k8s/domains/capabilities/infra/worker/overlays/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: event-bus
    - name: accounts-worker
    - name: capabilities-migrate
  wait: true
  timeout: 5m
