apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: spaces-api
spec:
  interval: 10m
  path: ./k8s/domains/spaces/infra/api/overlays/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: ingress
    - name: spaces-migrate
  wait: true
  timeout: 5m
