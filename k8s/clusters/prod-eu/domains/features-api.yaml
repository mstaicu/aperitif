apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: features-api
spec:
  interval: 10m
  path: ./k8s/domains/features/infra/api/overlays/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: ingress
    - name: observability
    - name: identity-api
    - name: features-migrate
  wait: true
  timeout: 5m
