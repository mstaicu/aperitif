apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: spaces-migrate
spec:
  interval: 10m
  path: ./k8s/domains/spaces/infra/migrate/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: aperitif-repo
  wait: true
  timeout: 5m
