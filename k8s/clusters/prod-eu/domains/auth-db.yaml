apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: auth-db
spec:
  interval: 10m
  path: ./k8s/domains/auth/infra/db/overlays/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: aperitif-repo
