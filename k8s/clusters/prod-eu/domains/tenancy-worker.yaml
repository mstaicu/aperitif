apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: tenancy-worker
spec:
  interval: 10m
  path: ./k8s/domains/tenancy/infra/worker/overlays/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: event-bus
    - name: tenancy-migrate
  wait: true
  timeout: 5m
