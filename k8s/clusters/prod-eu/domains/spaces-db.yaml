apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: spaces-db
spec:
  interval: 10m
  path: ./k8s/domains/spaces/infra/db/overlays/live
  prune: true
  sourceRef:
    kind: GitRepository
    name: aperitif-repo
