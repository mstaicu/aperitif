apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository

metadata:
  name: spaces-api
spec:
  image: docker.io/mdstaicu/spaces-api
  interval: 1m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository

metadata:
  name: spaces-migrate
spec:
  image: docker.io/mdstaicu/spaces-migrate
  interval: 1m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy

metadata:
  name: spaces-api-latest
spec:
  imageRepositoryRef:
    name: spaces-api
  filterTags:
    pattern: "^[a-f0-9]{7}$"
  policy:
    latest: {}
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy

metadata:
  name: spaces-migrate-latest
spec:
  imageRepositoryRef:
    name: spaces-migrate
  filterTags:
    pattern: "^[a-f0-9]{7}$"
  policy:
    latest: {}
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation

metadata:
  name: spaces
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  git:
    checkout:
      ref:
        branch: master
    commit:
      author:
        email: flux@tma.com
        name: FluxCD Bot
    push:
      branch: master
  update:
    path: ./k8s/domains/spaces/infra
    strategy: Setters
