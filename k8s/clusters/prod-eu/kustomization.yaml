apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - platform
  - domains

patches:
  - target:
      group: kustomize.toolkit.fluxcd.io
      version: v1
      kind: Kustomization
    patch: |-
      - op: add
        path: /metadata/namespace
        value: flux-system
