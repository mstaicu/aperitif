apiVersion: batch/v1
kind: Job

metadata:
  name: migrate
spec:
  backoffLimit: 4
  activeDeadlineSeconds: 300
  template:
    metadata:
      labels:
        app: tenancy-migrate
    spec:
      restartPolicy: Never

      containers:
        - name: migrate
          image: mdstaicu/tenancy-migrate

          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: tenancy-migrate-db
                  key: DATABASE_URL
