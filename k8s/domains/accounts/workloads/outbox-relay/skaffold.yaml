apiVersion: skaffold/v4beta14
kind: Config

metadata:
  name: accounts-outbox-relay

requires:
  - path: ../../../../platform/runtime/outbox-relay/skaffold.yaml
    configs: [outbox-relay]

manifests:
  kustomize:
    paths:
      - infra/overlays/ephemeral

deploy:
  kubectl: {}
