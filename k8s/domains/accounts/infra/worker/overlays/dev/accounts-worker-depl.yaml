apiVersion: apps/v1
kind: Deployment

metadata:
  name: accounts-worker-depl
spec:
  template:
    spec:
      containers:
        - name: accounts-worker
          env:
            - name: NATS_STREAM_REPLICAS
              value: "3"
