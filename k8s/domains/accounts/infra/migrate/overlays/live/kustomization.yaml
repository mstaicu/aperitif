apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: accounts

resources:
  - ../../base
  - accounts-migrate-db.yaml

images:
  - name: mdstaicu/accounts-migrate
    # This field is managed by this ImagePolicy: <namespace>:<imagepolicy-name>
    # meaning: flux-system:accounts-migrate-latest
    newTag: latest # {"$imagepolicy": "flux-system:accounts-migrate-latest"}
