apiVersion: v1
kind: Service

metadata:
  name: accounts-postgres
spec:
  type: ClusterIP
  selector:
    app: accounts-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
