apiVersion: batch/v1
kind: Job

metadata:
  name: migrations
spec:
  template:
    spec:
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: migrations
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          env:
            - name: FLYWAY_URL
              value: "jdbc:postgresql://postgres:5432/accounts?user=postgres&password=postgres"
