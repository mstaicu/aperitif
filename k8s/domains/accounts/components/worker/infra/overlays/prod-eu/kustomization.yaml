apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: accounts

resources:
  - ../../base

patches:
  - path: accounts-worker-depl.yaml

images:
  - name: ghcr.io/mstaicu/accounts-worker
    newTag: latest # {"$imagepolicy": "flux-system:accounts-worker:tag"}
    digest: "" # {"$imagepolicy": "flux-system:accounts-worker:digest"}
