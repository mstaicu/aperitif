apiVersion: traefik.io/v1alpha1
kind: IngressRoute

metadata:
  name: api-internal
spec:
  entryPoints:
    - http

  routes:
    - kind: Rule
      match: Path(`/v1/accounts`) || PathPrefix(`/v1/accounts/docs`)
      services:
        - name: api
          port: 3000
