apiVersion: batch/v1
kind: Job

metadata:
  name: migrations
spec:
  template:
    spec:
      containers:
        - name: migrations
          env:
            - name: FLYWAY_URL
              value: "jdbc:postgresql://documents-postgres:5432/documents?user=postgres&password=postgres"
