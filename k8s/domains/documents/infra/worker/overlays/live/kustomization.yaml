apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: documents

resources:
  - namespace.yaml
  - ../../base
  - documents-worker-db.yaml

patches:
  - path: documents-worker-depl.yaml

images:
  - name: mdstaicu/documents-worker
    newTag: latest
