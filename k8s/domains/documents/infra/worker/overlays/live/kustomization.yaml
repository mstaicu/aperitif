apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: documents

resources:
  - namespace.yaml
  - ../../base
  - documents-worker-db.yaml

images:
  - name: mdstaicu/documents-worker
    # This field is managed by this ImagePolicy: <namespace>:<imagepolicy-name>
    # meaning: flux-system:documents-worker-latest
    newTag: latest # {"$imagepolicy": "flux-system:documents-worker-latest"}
