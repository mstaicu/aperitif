apiVersion: apps/v1
kind: Deployment

metadata:
  name: documents-api-depl
spec:
  selector:
    matchLabels:
      app: documents-api
  replicas: 3
  template:
    metadata:
      labels:
        app: documents-api
    spec:
      terminationGracePeriodSeconds: 15
      serviceAccountName: documents-api
      # Defense in deep, disable this at the pod level
      # to prevent overwrites at the service account level
      automountServiceAccountToken: false

      containers:
        - name: documents-api
          image: mdstaicu/documents-api
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: documents-api-db
                  key: DATABASE_URL

            - name: IDENTITY_JWKS_URL
              value: http://traefik-srv.traefik.svc.cluster.local/.well-known/jwks.json

            - name: ACCESS_TOKEN_AUDIENCE
              value: aperitif-api

          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2

          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 2
