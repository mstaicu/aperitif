apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: documents-security-headers
spec:
  headers:
    contentTypeNosniff: true
    frameDeny: true
    referrerPolicy: no-referrer
    stsSeconds: 31536000
    stsIncludeSubdomains: false

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: documents-rate-limit
spec:
  rateLimit:
    average: 100
    period: 1s
    burst: 200

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: documents-concurrency-limit
spec:
  inFlightReq:
    amount: 100

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: documents-body-limit
spec:
  buffering:
    maxRequestBodyBytes: 1048576

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: documents-api-public
spec:
  chain:
    middlewares:
      - name: documents-security-headers
      - name: documents-rate-limit
      - name: documents-concurrency-limit
      - name: documents-body-limit
