apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: documents

resources:
  - namespace.yaml
  - ../../base
  - documents-migrate-db.yaml

images:
  - name: mdstaicu/documents-migrate
    # This field is managed by this ImagePolicy: <namespace>:<imagepolicy-name>
    # meaning: flux-system:documents-migrate-latest
    newTag: latest # {"$imagepolicy": "flux-system:documents-migrate-latest"}
