apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: documents

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: documents-migrate-job.yaml

images:
  - name: mdstaicu/documents-migrate
    newTag: latest
