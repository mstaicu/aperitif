apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: documents

resources:
  - namespace.yaml
  - ../../base
  - documents-ui-ingress.yaml

patches:
  - path: documents-ui-depl.yaml

images:
  - name: mdstaicu/documents-ui
    newTag: latest
