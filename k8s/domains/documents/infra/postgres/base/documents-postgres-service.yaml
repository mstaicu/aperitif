apiVersion: v1
kind: Service

metadata:
  name: documents-postgres
spec:
  type: ClusterIP
  selector:
    app: documents-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
