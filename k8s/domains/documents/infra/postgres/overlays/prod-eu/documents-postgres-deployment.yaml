apiVersion: apps/v1
kind: Deployment

metadata:
  name: documents-postgres
spec:
  template:
    spec:
      containers:
        - name: documents-postgres
          env:
            - name: POSTGRES_PASSWORD
              value: postgres
