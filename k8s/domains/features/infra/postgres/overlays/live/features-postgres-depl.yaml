apiVersion: apps/v1
kind: Deployment

metadata:
  name: features-postgres-depl
spec:
  template:
    spec:
      containers:
        - name: features-postgres
          env:
            - name: POSTGRES_PASSWORD
              value: postgres

          volumeMounts:
            - name: postgres-init
              mountPath: /docker-entrypoint-initdb.d/001-features-init.sql
              subPath: features-postgres-init.sql
              readOnly: true

      volumes:
        - name: postgres-init
          configMap:
            name: features-postgres-init
