apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: features

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: features-postgres-depl.yaml

configMapGenerator:
  - name: features-postgres-init
    files:
      - features-postgres-init.sql
