apiVersion: apps/v1
kind: Deployment

metadata:
  name: features-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: features-postgres
  template:
    metadata:
      labels:
        app: features-postgres
    spec:
      containers:
        - name: features-postgres
          image: docker.io/postgres:18
          env:
            - name: POSTGRES_DB
              value: features

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d features
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d features
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: v1
kind: Service

metadata:
  name: postgres-srv
spec:
  type: ClusterIP
  selector:
    app: features-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
