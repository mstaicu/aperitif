apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: features-api-public
spec:
  hostnames:
    - api.tma.com

  parentRefs:
    - name: traefik-pgw
      namespace: traefik
      sectionName: https-api

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /v1/features

      backendRefs:
        - name: features-api-srv
          port: 3000

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: features-api-internal
spec:
  parentRefs:
    - name: traefik-igw
      namespace: traefik
      sectionName: http

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /v1/features

      backendRefs:
        - name: features-api-srv
          port: 3000
