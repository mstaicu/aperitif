apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: features

resources:
  - namespace.yaml
  - ../../base

generators:
  - features-worker-ksops-generator.yaml

patches:
  - path: features-worker-depl.yaml
