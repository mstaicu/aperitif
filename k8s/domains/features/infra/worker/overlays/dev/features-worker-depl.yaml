apiVersion: apps/v1
kind: Deployment

metadata:
  name: features-worker-depl
spec:
  template:
    spec:
      containers:
        - name: features-worker
          env:
            - name: NATS_STREAM_REPLICAS
              value: "3"
