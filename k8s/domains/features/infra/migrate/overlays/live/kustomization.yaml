apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: features

resources:
  - namespace.yaml
  - ../../base
  - features-migrate-db.yaml

images:
  - name: mdstaicu/features-migrate
    # This field is managed by this ImagePolicy: <namespace>:<imagepolicy-name>
    # meaning: flux-system:features-migrate-latest
    newTag: latest # {"$imagepolicy": "flux-system:features-migrate-latest"}
