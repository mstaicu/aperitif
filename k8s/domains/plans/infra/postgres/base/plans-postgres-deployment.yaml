apiVersion: apps/v1
kind: Deployment

metadata:
  name: plans-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plans-postgres
  template:
    metadata:
      labels:
        app: plans-postgres
    spec:
      automountServiceAccountToken: false
      containers:
        - name: plans-postgres
          image: docker.io/postgres:18
          env:
            - name: POSTGRES_DB
              value: plans

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d plans
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d plans
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
