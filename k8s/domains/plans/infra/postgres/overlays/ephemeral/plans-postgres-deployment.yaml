apiVersion: apps/v1
kind: Deployment

metadata:
  name: plans-postgres
spec:
  template:
    spec:
      containers:
        - name: plans-postgres
          env:
            - name: POSTGRES_PASSWORD
              value: postgres
