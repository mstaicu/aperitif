apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: identity

resources:
  - ../../base

patches:
  - path: identity-migrate-job.yaml

images:
  - name: mdstaicu/identity-migrate
    newTag: latest
