{
  "type": "module",
  "scripts": {
    "build": "esbuild app/pages/*.client.ts --bundle --format=esm --outdir=public/identity/assets --entry-names=[name] --minify --sourcemap && esbuild app/platform/observability/otel.ts --bundle --platform=node --format=esm --outfile=dist/otel.js --packages=external && esbuild server.ts --bundle --platform=node --format=esm --outfile=dist/server.js --packages=external",
    "dev": "sh ./dev.sh",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/instrumentation-http": "^0.218.0",
    "@opentelemetry/instrumentation-undici": "^0.28.0",
    "@opentelemetry/sdk-node": "^0.218.0",
    "@simplewebauthn/browser": "^13.3.0",
    "remix": "^3.0.0-beta.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
