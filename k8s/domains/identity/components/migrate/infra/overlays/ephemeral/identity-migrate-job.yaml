apiVersion: batch/v1
kind: Job

metadata:
  name: migrate
spec:
  template:
    spec:
      containers:
        - name: migrate
          env:
            - name: FLYWAY_URL
              value: "jdbc:postgresql://identity-postgres:5432/identity?user=postgres&password=postgres"
