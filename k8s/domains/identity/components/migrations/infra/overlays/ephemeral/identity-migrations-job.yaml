apiVersion: batch/v1
kind: Job

metadata:
  name: migrations
spec:
  template:
    spec:
      containers:
        - name: migrations
          env:
            - name: FLYWAY_URL
              value: "jdbc:postgresql://identity-postgres:5432/identity?user=postgres&password=postgres"
