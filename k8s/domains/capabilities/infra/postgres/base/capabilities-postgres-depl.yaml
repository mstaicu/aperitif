apiVersion: apps/v1
kind: Deployment

metadata:
  name: capabilities-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: capabilities-postgres
  template:
    metadata:
      labels:
        app: capabilities-postgres
    spec:
      containers:
        - name: capabilities-postgres
          image: docker.io/postgres:18
          env:
            - name: POSTGRES_DB
              value: capabilities

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d capabilities
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d capabilities
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: v1
kind: Service

metadata:
  name: postgres-srv
spec:
  type: ClusterIP
  selector:
    app: capabilities-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
