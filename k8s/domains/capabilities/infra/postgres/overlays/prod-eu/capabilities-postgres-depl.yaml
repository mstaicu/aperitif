apiVersion: apps/v1
kind: Deployment

metadata:
  name: capabilities-postgres-depl
spec:
  template:
    spec:
      containers:
        - name: capabilities-postgres
          env:
            - name: POSTGRES_PASSWORD
              value: postgres
