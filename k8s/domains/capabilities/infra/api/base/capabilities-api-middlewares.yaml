apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: capabilities-security-headers
spec:
  headers:
    contentTypeNosniff: true
    frameDeny: true
    referrerPolicy: no-referrer
    stsSeconds: 31536000
    stsIncludeSubdomains: false

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: capabilities-rate-limit
spec:
  rateLimit:
    average: 100
    period: 1s
    burst: 200

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: capabilities-concurrency-limit
spec:
  inFlightReq:
    amount: 100

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: capabilities-body-limit
spec:
  buffering:
    maxRequestBodyBytes: 1048576

---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: capabilities-api-public
spec:
  chain:
    middlewares:
      - name: capabilities-security-headers
      - name: capabilities-rate-limit
      - name: capabilities-concurrency-limit
      - name: capabilities-body-limit
