apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: capabilities

resources:
  - namespace.yaml
  - ../../base
  - capabilities-worker-db.yaml

patches:
  - path: capabilities-worker-depl.yaml

images:
  - name: mdstaicu/capabilities-worker
    newTag: latest
