apiVersion: apps/v1
kind: Deployment

metadata:
  name: capabilities-worker-depl
spec:
  template:
    spec:
      containers:
        - name: capabilities-worker
          env:
            - name: NATS_STREAM_REPLICAS
              value: "3"
