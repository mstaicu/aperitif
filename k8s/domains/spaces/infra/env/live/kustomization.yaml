apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spaces

resources:
  - ../../api
  - spaces-api-ingress.yaml
  - namespace.yaml

# TODO: Add and encrypt secret
# secretGenerator:
#   - name: spaces-db
#     literals:
#       - DATABASE_URL=postgres://postgres@postgres-srv:5432/spaces?sslmode=disable

images:
  - name: mdstaicu/spaces-api
    # This field is managed by this ImagePolicy: <namespace>:<imagepolicy-name>
    # meaning: flux-system:spaces-api-latest
    newTag: latest # {"$imagepolicy": "flux-system:spaces-api-latest"}
