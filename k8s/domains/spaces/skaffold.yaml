apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: spaces-db-local

manifests:
  kustomize:
    paths:
      - infra/db/overlays/local

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: spaces-migrate-local

build:
  local:
    push: false

  artifacts:
    - image: mdstaicu/spaces-migrate
      context: migrations
      docker:
        dockerfile: Dockerfile

manifests:
  kustomize:
    paths:
      - infra/migrate/overlays/local

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: spaces-api-local

build:
  local:
    push: false

  artifacts:
    - image: mdstaicu/spaces-api
      context: api
      docker:
        dockerfile: Dockerfile
        target: dev

      sync:
        manual:
          - src: src/**/*
            dest: /home/node/app

manifests:
  kustomize:
    paths:
      - infra/api/overlays/local
    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}
