apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: entitlements-postgres

manifests:
  kustomize:
    paths:
      - infra/postgres/overlays/ephemeral

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: entitlements-migrate

build:
  local:
    push: false

  artifacts:
    - image: ghcr.io/mstaicu/entitlements-migrate
      context: components/migrate
      docker:
        dockerfile: Dockerfile

manifests:
  kustomize:
    paths:
      - components/migrate/infra/overlays/ephemeral
    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: entitlements-api

build:
  local:
    push: false

  artifacts:
    - image: ghcr.io/mstaicu/entitlements-api
      context: components/api
      docker:
        dockerfile: Dockerfile
        target: dev

      sync:
        manual:
          - src: src/**/*
            dest: /home/node/app

manifests:
  kustomize:
    paths:
      - components/api/infra/overlays/ephemeral
    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: entitlements-outbox-publisher

build:
  local:
    push: false

  artifacts:
    - image: ghcr.io/mstaicu/entitlements-outbox-publisher
      context: components/outbox-publisher
      docker:
        dockerfile: Dockerfile
        target: dev

      sync:
        manual:
          - src: src/**/*
            dest: /home/node/app

manifests:
  kustomize:
    paths:
      - components/outbox-publisher/infra/overlays/ephemeral
    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: entitlements-accounts-projector

build:
  local:
    push: false

  artifacts:
    - image: ghcr.io/mstaicu/entitlements-accounts-projector
      context: components/accounts-projector
      docker:
        dockerfile: Dockerfile
        target: dev

      sync:
        manual:
          - src: src/**/*
            dest: /home/node/app

manifests:
  kustomize:
    paths:
      - components/accounts-projector/infra/overlays/ephemeral
    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}
