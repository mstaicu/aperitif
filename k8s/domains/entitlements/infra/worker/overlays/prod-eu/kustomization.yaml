apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: entitlements

resources:
  - ../../base

patches:
  - path: entitlements-worker-depl.yaml

images:
  - name: ghcr.io/mstaicu/entitlements-worker
    newTag: latest
