apiVersion: apps/v1
kind: Deployment

metadata:
  name: entitlements-postgres
spec:
  template:
    spec:
      containers:
        - name: entitlements-postgres
          env:
            - name: POSTGRES_PASSWORD
              value: postgres
