apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: entitlements

resources:
  - ../../base

patches:
  - path: entitlements-migrate-job.yaml

images:
  - name: ghcr.io/mstaicu/entitlements-migrate
    newTag: latest
