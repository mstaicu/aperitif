apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: entitlements

resources:
  - ../../base

patches:
  - path: outbox-publisher-depl.yaml

images:
  - name: ghcr.io/mstaicu/entitlements-outbox-publisher
    newTag: latest # {"$imagepolicy": "flux-system:entitlements-outbox-publisher:tag"}
    digest: "" # {"$imagepolicy": "flux-system:entitlements-outbox-publisher:digest"}
