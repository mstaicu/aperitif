apiVersion: batch/v1
kind: Job

metadata:
  name: migrate
spec:
  template:
    spec:
      containers:
        - name: migrate
          env:
            - name: FLYWAY_URL
              value: "jdbc:postgresql://entitlements-postgres:5432/entitlements?user=postgres&password=postgres"
