apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: identities-api-public
spec:
  parentRefs:
    - name: traefik-pgw
      namespace: traefik
      sectionName: https

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /identities

      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /

      backendRefs:
        - name: identities-api-srv
          port: 3000

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: identities-jwks-public
spec:
  parentRefs:
    - name: traefik-pgw
      namespace: traefik
      sectionName: https

  rules:
    - matches:
        - path:
            type: Exact
            value: /.well-known/jwks.json

      backendRefs:
        - name: identities-api-srv
          port: 3000

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: identities-api-internal
spec:
  parentRefs:
    - name: traefik-igw
      namespace: traefik
      sectionName: http

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /identities

      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /

      backendRefs:
        - name: identities-api-srv
          port: 3000

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: identities-jwks-internal
spec:
  parentRefs:
    - name: traefik-igw
      namespace: traefik
      sectionName: http

  rules:
    - matches:
        - path:
            type: Exact
            value: /.well-known/jwks.json

      backendRefs:
        - name: identities-api-srv
          port: 3000
