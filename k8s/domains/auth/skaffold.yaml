apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: auth-db-local

manifests:
  kustomize:
    paths:
      - infra/db/overlays/local

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: auth-migrate-local

build:
  local:
    push: false

  artifacts:
    - image: mdstaicu/auth-migrate
      context: migrations
      docker:
        dockerfile: Dockerfile

manifests:
  kustomize:
    paths:
      - infra/migrate/overlays/local

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: auth-api-local

build:
  local:
    push: false

  artifacts:
    - image: mdstaicu/auth-api
      context: api
      docker:
        dockerfile: Dockerfile
        target: dev

      sync:
        manual:
          - src: src/**/*
            dest: /home/node/app

manifests:
  kustomize:
    paths:
      - infra/api/overlays/local
    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}
