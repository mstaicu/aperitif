apiVersion: apps/v1
kind: Deployment

metadata:
  name: auth-nats-depl
spec:
  replicas: 1
  template:
    spec:
      containers:
        - image: nats:2.12-alpine
          args:
            - "-js"

---
kind: Service
metadata:
  name: auth-nats-srv
