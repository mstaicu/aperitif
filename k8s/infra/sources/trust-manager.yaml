apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository

metadata:
  name: trust-manager-oci
  namespace: flux-system
spec:
  url: oci://registry.venafi.cloud/charts/trust-manager
  ref:
    tag: v0.19.0
  interval: 1h
  # layerSelector:
  #   mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
  #   operation: copy
