apiVersion: traefik.io/v1alpha1
kind: IngressRoute

metadata:
  name: identity-ingress
spec:
  entryPoints:
    - https
  routes:
    - match: Host(`$PR_DOMAIN`) && PathPrefix(`/api/v1/identity`)
      kind: Rule
      priority: 69
      middlewares:
        - name: identity-strip-prefix
      services:
        - name: identity-srv
          port: 3000
  tls:
    options:
      name: tls-options
      namespace: traefik
---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: identity-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /api/v1/identity
