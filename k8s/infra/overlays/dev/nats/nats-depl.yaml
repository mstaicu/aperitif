apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: nats-depl
spec:
  template:
    spec:
      affinity: null
      containers:
        - name: nats
          lifecycle:
            preStop:
              exec:
                command:
                  - nats-server
                  - -sl=ldm=/tmp/nats.pid
          volumeMounts:
            - name: nats-volume-resolver
              mountPath: /etc/resolver.conf
              subPath: resolver.conf
            - name: nats-pvc
              mountPath: /data
      volumes:
        - name: nats-volume-resolver
          secret:
            secretName: nats-resolver-secret # Holds the memory resolver

  volumeClaimTemplates:
    - metadata:
        name: nats-pvc
      spec:
        storageClassName: hostpath
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
