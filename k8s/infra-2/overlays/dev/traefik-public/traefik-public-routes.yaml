apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: client-route-https
spec:
  entryPoints:
    - https
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      middlewares:
        - name: tls
      services:
        - name: auth-srv
          port: 3000
  tls:
    secretName: certs-secret
    options:
      name: tls-options
