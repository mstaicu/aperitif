#
# skaffold run -m ingress-crds
# skaffold run -m ingress
# skaffold run -m event-bus
#
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: bootstrap

requires:
  - path: platform

---
#
# skaffold dev -m app
#
# skaffold dev -m app -p local
# skaffold dev -m app -p preview
#
# skaffold dev -m auth -p preview
# skaffold dev -m auth -p local
#
apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: app

requires:
  - path: domains