apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: k-s
build:
  artifacts:
    - image: mdstaicu/auth
      context: auth
      docker:
        dockerfile: Dockerfile
        target: dev
      sync:
        manual:
          - src: src/**/*.js
            dest: .
  local:
    push: false
manifests:
  rawYaml:
    - infra/auth/auth-depl.yaml
