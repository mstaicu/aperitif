name: CI / Entitlements

on:
  pull_request:
    paths:
      - "domains/entitlements/**"

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci-domain.yaml
    with:
      domain: entitlements
    secrets:
      EPHEMERAL_SOPS_AGE_KEY: ${{ secrets.EPHEMERAL_SOPS_AGE_KEY }}
