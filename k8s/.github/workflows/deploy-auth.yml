name: deploy-auth

on:
  push:
    branches:
      - master
    paths:
      - "auth/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd auth && docker build -t mdstaicu/auth .
      - run: docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - uses: digitalocean/action-doctl@v2
      - run: docker push mdstaicu/auth
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
      # Get the name of the cluster from DigitalOcean, in this case "k8s-ticketing"
      - run: doctl kubernetes cluster kubeconfig save k8s-ticketing
      # Use the name of the deployment as specified in the depl file, in the metadata section, in this case "auth-depl"
      - run: kubectl rollout restart deployment auth-depl
