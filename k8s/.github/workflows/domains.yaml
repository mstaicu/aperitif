on:
  pull_request:
    paths:
      - "domains/**"

permissions:
  contents: read
  pull-requests: read

jobs:
  integration:
    runs-on: ubuntu-latest

    concurrency:
      group: pull-request-${{ github.event.pull_request.number || github.ref }}
      cancel-in-progress: true

    env:
      CLUSTER_NAME: domains-${{ github.event.pull_request.number }}
      DOMAIN: tma.com
      KSOPS_VERSION: 4.5.1
      MKCERT_VERSION: v1.4.4
      SKAFFOLD_VERSION: v2.17.0
      SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}
      SOPS_VERSION: v3.12.2

    steps:
      # Fetch the PR sources so Make, Skaffold, Kustomize, and domain files are available.
      - name: checkout
        uses: actions/checkout@v6

      # Detect which top-level domains changed so the workflow deploys only the affected domain targets.
      - name: select domains
        uses: actions/github-script@v8
        with:
          script: |
            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            });

            const domains = [...new Set(
              files
                .map((file) => file.filename.match(/^domains\/([^/]+)\//)?.[1])
                .filter(Boolean)
            )].sort();

            if (domains.length === 0) {
              core.setFailed("No domain changes found.");
              return;
            }

            core.exportVariable("CHANGED_DOMAINS", domains.join(" "));

      # Fail early if a changed domain does not have a matching Make deploy target.
      - name: check domain targets
        run: |
          for domain in $CHANGED_DOMAINS; do
            make -n "$domain" >/dev/null
          done

      # Dev overlays use SOPS/KSOPS, so CI needs the repository age key secret.
      - name: check sops age key
        if: env.SOPS_AGE_KEY == ''
        run: |
          echo "::error::SOPS_AGE_KEY repository secret is required for encrypted dev overlays."
          exit 1

      # Create an isolated disposable Kubernetes cluster for this PR run.
      - name: create kind cluster
        uses: helm/kind-action@v1.14.0
        with:
          cluster_name: ${{ env.CLUSTER_NAME }}
          wait: 120s

      # Install kubectl after cluster creation so later steps can inspect and deploy to the cluster.
      - name: setup kubectl
        uses: azure/setup-kubectl@v4.0.1

      # Make the target cluster explicit before Make and Skaffold deploy anything.
      - name: use kind context
        run: |
          kubectl config use-context "kind-${CLUSTER_NAME}"
          kubectl config current-context

      # Install standalone kustomize because KSOPS-enabled overlays need kustomize plugin support.
      - name: setup kustomize
        uses: imranismail/setup-kustomize@v2.1.0

      # Install the exact CLI tools used by the Make/Skaffold dev deployment path.
      - name: install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3-tools

          curl -fsSL \
            -o skaffold \
            "https://storage.googleapis.com/skaffold/releases/${SKAFFOLD_VERSION}/skaffold-linux-amd64"
          chmod +x skaffold
          sudo mv skaffold /usr/local/bin/skaffold

          curl -fsSL \
            -o mkcert \
            "https://github.com/FiloSottile/mkcert/releases/download/${MKCERT_VERSION}/mkcert-${MKCERT_VERSION}-linux-amd64"
          chmod +x mkcert
          sudo mv mkcert /usr/local/bin/mkcert

          curl -fsSL \
            -o sops \
            "https://github.com/getsops/sops/releases/download/${SOPS_VERSION}/sops-${SOPS_VERSION}.linux.amd64"
          chmod +x sops
          sudo mv sops /usr/local/bin/sops

          curl -fsSL \
            -o ksops.tar.gz \
            "https://github.com/viaduct-ai/kustomize-sops/releases/download/v${KSOPS_VERSION}/ksops_${KSOPS_VERSION}_Linux_x86_64.tar.gz"
          tar -xzf ksops.tar.gz
          sudo mv ksops /usr/local/bin/ksops

      # Tell Skaffold this context is local so built images are loaded into kind instead of pushed.
      - name: configure skaffold
        run: |
          skaffold config set --kube-context "kind-${CLUSTER_NAME}" local-cluster true

      # Reuse the same domain Make targets used locally; they deploy dependencies, migrations, APIs, and workers.
      - name: deploy domain
        run: |
          for domain in $CHANGED_DOMAINS; do
            make "$domain"
          done

      # Expose Traefik from kind to the runner so smoke checks hit the real ingress path.
      - name: expose ingress
        run: |
          kubectl -n traefik port-forward svc/traefik-lb 8443:443 > /tmp/traefik-port-forward.log 2>&1 &
          echo "$!" > /tmp/traefik-port-forward.pid
          timeout 30 bash -c 'until : > /dev/tcp/127.0.0.1/8443; do sleep 1; done'

      # Check each changed domain through Traefik using the minimal stable endpoint for that domain.
      - name: smoke domains
        run: |
          for domain in $CHANGED_DOMAINS; do
            case "$domain" in
              identities)
                smoke_path="/.well-known/jwks.json"
                smoke_status="200"
                ;;
              tenancy)
                smoke_path="/tenancy/v1/accounts"
                smoke_status="401"
                ;;
              *)
                echo "::error::No smoke check configured for domain: ${domain}"
                exit 1
                ;;
            esac

            status="$(curl \
              -ksS \
              -o /tmp/domain-smoke-response.txt \
              -w "%{http_code}" \
              --resolve "${DOMAIN}:8443:127.0.0.1" \
              "https://${DOMAIN}:8443${smoke_path}")"

            if [ "$status" != "$smoke_status" ]; then
              echo "Expected ${domain} HTTP ${smoke_status}, got HTTP ${status}"
              cat /tmp/domain-smoke-response.txt
              exit 1
            fi
          done

      # Always tear down background processes and the disposable kind cluster.
      - name: cleanup
        if: always()
        run: |
          set +e

          if [ -f /tmp/traefik-port-forward.pid ]; then
            kill "$(cat /tmp/traefik-port-forward.pid)"
          fi

          kind delete cluster --name "$CLUSTER_NAME"
