name: integration / identity

on:
  pull_request:
    paths:
      - "domains/identity/**"

permissions:
  contents: read

jobs:
  integration:
    uses: ./.github/workflows/integration.yaml
    with:
      domain: identity
    secrets:
      SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}
