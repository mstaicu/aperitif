name: deployment / capabilities / migrate

on:
  push:
    branches:
      - master
    paths:
      - "domains/capabilities/packages/migrate/Dockerfile"
      - "domains/capabilities/packages/migrate/migrations/**"

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    concurrency:
      group: deployment-master
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v6
        with:
          ref: master
          fetch-depth: 0

      - uses: imranismail/setup-kustomize@v2.1.0
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - id: build
        uses: docker/build-push-action@v6
        with:
          context: domains/capabilities/packages/migrate
          target: prod
          push: true
          tags: mdstaicu/capabilities-migrate:${{ hashFiles('domains/capabilities/packages/migrate/Dockerfile', 'domains/capabilities/packages/migrate/migrations/**') }}

      - name: update manifest
        working-directory: domains/capabilities/infra/migrate/overlays/live
        run: kustomize edit set image "mdstaicu/capabilities-migrate=mdstaicu/capabilities-migrate@${{ steps.build.outputs.digest }}"

      - name: commit manifest
        shell: bash
        run: |
          set -euo pipefail

          manifest="domains/capabilities/infra/migrate/overlays/live/kustomization.yaml"

          if git diff --quiet -- "$manifest"; then
            echo "No manifest changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$manifest"
          git commit -m "Deploy capabilities migrate ${GITHUB_SHA::12}"
          git push
