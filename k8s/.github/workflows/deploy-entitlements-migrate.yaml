name: deployment / entitlements / migrate

on:
  push:
    branches:
      - master
    paths:
      - "domains/entitlements/packages/database/Dockerfile"
      - "domains/entitlements/packages/database/.dockerignore"
      - "domains/entitlements/packages/database/sql/**"

permissions:
  contents: write

jobs:
  deploy:
    uses: ./.github/workflows/deployment.yaml
    with:
      context: domains/entitlements/packages/database
      domain: entitlements
      image: mdstaicu/entitlements-migrate
      manifest: domains/entitlements/infra/migrate/overlays/prod-eu
      unit: migrate
    secrets:
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
