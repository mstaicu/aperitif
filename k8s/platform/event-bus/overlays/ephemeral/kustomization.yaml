apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nats

resources:
  - ../../base
  - networkpolicy.yaml
  - namespace.yaml

patches:
  - path: statefulset.yaml
  - path: client-service.yaml
