apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nats

resources:
  - ../../base

configMapGenerator:
  - name: nats-config
    files:
      - nats.conf=nats.conf

generatorOptions:
  disableNameSuffixHash: true

patches:
  - path: nats-depl.yaml
  - path: nats-srv.yaml
