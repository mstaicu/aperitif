apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

configMapGenerator:
  - name: nats-config
    files:
      - nats.conf=nats.conf

generatorOptions:
  disableNameSuffixHash: true

patches:
  - path: nats-depl.yaml
