apiVersion: skaffold/v4beta11
kind: Config

metadata:
  name: observability

manifests:
  kustomize:
    paths:
      - overlays/ephemeral

    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}
