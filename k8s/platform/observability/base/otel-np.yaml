apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: default-deny-otel-collector
spec:
  podSelector:
    matchLabels:
      app: otel-collector
  policyTypes:
    - Ingress

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: allow-domain-apis-to-otel-http
spec:
  podSelector:
    matchLabels:
      app: otel-collector
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: identity
          podSelector:
            matchLabels:
              app: identity-api
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: identity
          podSelector:
            matchLabels:
              app: identity-ui
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: accounts
          podSelector:
            matchLabels:
              app: accounts-api
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: capabilities
          podSelector:
            matchLabels:
              app: capabilities-api
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: documents
          podSelector:
            matchLabels:
              app: documents-api
      ports:
        - protocol: TCP
          port: 4318

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: allow-traefik-to-otel-grpc
spec:
  podSelector:
    matchLabels:
      app: otel-collector
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: traefik
          podSelector:
            matchLabels:
              app: traefik
      ports:
        - protocol: TCP
          port: 4317

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: allow-otel-agent-to-otel-grpc
spec:
  podSelector:
    matchLabels:
      app: otel-collector
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: otel-agent
      ports:
        - protocol: TCP
          port: 4317
