apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole

metadata:
  name: otel-agent
rules:
  - apiGroups:
      - ""
    resources:
      - nodes/stats
      # Required to read container limits for request/limit utilization metrics.
      - nodes/pods
    verbs:
      - get
