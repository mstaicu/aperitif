apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole

metadata:
  name: otel-agent
rules:
  - apiGroups:
      - ""
    resources:
      - nodes/stats
    verbs:
      - get
      - list
      - watch
