apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linkerd

resources:
  - ../../base
  - linkerd-identity-trust-roots.yaml
  - linkerd-ns.yaml
  - linkerd-webhook-trust-roots.yaml

patches:
  - path: linkerd-webhook-ca-bundle-patches.yaml

generators:
  - linkerd-ksops-generator.yaml
