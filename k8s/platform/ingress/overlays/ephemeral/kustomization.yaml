apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yaml
  - ../../base
  - traefik-ingress.yaml
  - traefik-tlsstore.yaml

patches:
  - path: traefik-depl.yaml
  - path: traefik-lb.yaml
