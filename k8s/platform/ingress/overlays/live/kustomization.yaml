apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yaml
  - ../../base
  - traefik-cf.yaml
  - traefik-do-pvc.yaml

patches:
  - path: traefik-depl.yaml
