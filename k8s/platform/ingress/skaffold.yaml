apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: ingress-crds

manifests:
  kustomize:
    paths:
      - crds

deploy:
  kubectl: {}

---
apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: ingress

manifests:
  kustomize:
    paths:
      - overlays/dev

    buildArgs:
      - --enable-alpha-plugins
      - --enable-exec

deploy:
  kubectl: {}
