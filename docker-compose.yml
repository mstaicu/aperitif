version: "3.1"

services:

  traefik:
    image: traefik:2.4.1

  # TODO: Figure out why 'build' directives don't overrride 'image' directives
  api:
    image: mdstaicu/api

  postgres:
    image: postgres