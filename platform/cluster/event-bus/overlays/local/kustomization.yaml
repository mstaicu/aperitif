apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nats

resources:
  - ../../base
  - namespace.yaml

configMapGenerator:
  - name: nats-config
    files:
      - nats.conf=nats.conf

patches:
  - path: service.yaml
