apiVersion: v1
kind: Service

metadata:
  name: nats-headless
spec:
  clusterIP: None
  selector:
    app: nats
  publishNotReadyAddresses: true
  ports:
    - name: nats-cluster
      protocol: TCP
      port: 6222
      targetPort: 6222
