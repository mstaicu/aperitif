apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - ../../base
  - service.yaml
  - cloudflare-dns-secret.yaml
  - acme-persistentvolumeclaim.yaml

patches:
  - path: deployment.yaml
