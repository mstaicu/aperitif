apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auth

resources:
  - namespace.yaml
  - ../../base
  - networkpolicy.yaml
  - ingress.yaml

patches:
  - path: deployment.yaml
